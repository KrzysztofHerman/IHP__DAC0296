#==========================================================================
# Copyright 2025 IHP PDK Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
# SPDX-License-Identifier: Apache-2.0
#==========================================================================

name: Physical Verification

on: 
    push:
    pull_request:
        branches: [ main]
    workflow_dispatch:

jobs:
    drc:
        runs-on: ubuntu-22.04
        steps:
            -   name: Checkout
                uses: actions/checkout@v4
            -   name: Install system dependencies
                run: |
                    sudo apt-get update -qq -y
                    sudo apt-get install -y jq python3-pip
            -   name: Download latest KLayout (Ubuntu amd64)
                run: |
                    UBUNTU_VERSION=$(lsb_release -rs | cut -d. -f1)
                    KLAYOUT_URL=$(grep -o "https://www\.klayout\.org/downloads/Ubuntu-${UBUNTU_VERSION}/[^\"]*amd64\.deb" link | head -n 1)
                    if [[ -z "$KLAYOUT_URL" ]]; then
                        echo "KLayout .deb URL not found in link for Ubuntu ${UBUNTU_VERSION}"
                        exit 1
                    fi
                    curl -fL -o /tmp/klayout_amd64.deb "$KLAYOUT_URL"
                    if ! dpkg-deb --info /tmp/klayout_amd64.deb >/dev/null 2>&1; then
                        echo "Downloaded file is not a valid Debian archive"
                        exit 1
                    fi
            -   name: Install KLayout
                run: |
                    sudo dpkg -i /tmp/klayout_amd64.deb || true
                    sudo apt-get install -f -y
                    klayout -v
            -   name: Install IHP-Open-PDK Python requirements
                run: |
                    python3 -m pip install --upgrade pip
                    python3 -m pip install -r https://raw.githubusercontent.com/IHP-GmbH/IHP-Open-PDK/main/requirements.txt
            -   name: Fetch IHP DRC deck and dependencies
                run: |
                    git clone --depth 1 --filter=blob:none --sparse https://github.com/IHP-GmbH/IHP-Open-PDK.git /tmp/ihp-open-pdk
                    git -C /tmp/ihp-open-pdk sparse-checkout set --no-cone /ihp-sg13g2/libs.tech/klayout/tech/drc/ /ihp-sg13g2/libs.tech/klayout/tech/lvs/ /ihp-sg13g2/libs.tech/klayout/python/sg13g2_pycell_lib/sg13g2_tech_mod.json
            -   name: Run DRC on release GDS
                run: |
                    GDS_PATH=$(jq -r '.release.gds' doc/info.json)
                    if [[ -z "$GDS_PATH" || "$GDS_PATH" == "null" ]]; then
                        echo "release.gds not found in doc/info.json"
                        exit 1
                    fi
                    mkdir -p /tmp/drc-results
                    python3 /tmp/ihp-open-pdk/ihp-sg13g2/libs.tech/klayout/tech/drc/run_drc.py --path="$GDS_PATH" --run_dir=/tmp/drc-results --mp=1 --precheck_drc
                shell: bash
            -   name: Run LVS on release netlist (if present)
                run: |
                    GDS_PATH=$(jq -r '.release.gds' doc/info.json)
                    NETLIST_PATH=$(jq -r '.release.netlist' doc/info.json)
                    if [[ -z "$NETLIST_PATH" || "$NETLIST_PATH" == "null" ]]; then
                        echo "release.netlist not found in doc/info.json, skipping LVS"
                        exit 0
                    fi
                    if [[ ! -f "$NETLIST_PATH" ]]; then
                        echo "Netlist file not found at $NETLIST_PATH, skipping LVS"
                        exit 0
                    fi
                    mkdir -p /tmp/lvs-results
                    python3 /tmp/ihp-open-pdk/ihp-sg13g2/libs.tech/klayout/tech/lvs/run_lvs.py --layout="$GDS_PATH" --netlist="$NETLIST_PATH" --run_dir=/tmp/lvs-results
                shell: bash
            -   name: Archive DRC Results
                uses: actions/upload-artifact@v4
                if: always()
                with:
                    name: DRC-Results
                    path: /tmp/drc-results
            -   name: Archive LVS Results
                uses: actions/upload-artifact@v4
                if: always()
                with:
                    name: LVS-Results
                    path: /tmp/lvs-results
